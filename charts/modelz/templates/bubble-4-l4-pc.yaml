{{- if .Values.bubble.enabled }}
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: {{ .Values.priorityClass.bubbleGPU4L4 | quote }}
  labels:
    app: {{ template "modelz.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: {{ .Values.priorityClass.bubbleGPU4L4 | quote }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
value: {{ .Values.priorityClass.bubbleGPU4L4Value }}
globalDefault: false
description: "This priority class should be used for bubble 4 T4 pods only."
{{- end }}
