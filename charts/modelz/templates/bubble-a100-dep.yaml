{{- if .Values.bubble.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.priorityClass.bubbleGPUA100 }}
  labels:
    app: {{ template "modelz.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: {{ .Values.priorityClass.bubbleGPUA100 }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
spec:
  replicas: 0
  selector:
    matchLabels:
      app: {{ .Values.priorityClass.bubbleGPUA100 }}
  template:
    metadata:
      labels:
        service: {{ .Values.priorityClass.bubbleGPUA100 }}
        app: {{ .Values.priorityClass.bubbleGPUA100 }}
    spec:
      priorityClassName: {{ .Values.priorityClass.bubbleGPUA100 }}
      containers:
      - image: {{ .Values.bubble.image }}
        imagePullPolicy: IfNotPresent
        name: victims
        resources:
          requests:
            cpu: 9
            memory: 60Gi
            nvidia.com/gpu: 1
          limits:
            cpu: 12
            memory: 85Gi
            nvidia.com/gpu: 1
      nodeSelector:
        ai.tensorchord.server-resource: {{ .Values.bubble.gpuA100Config.serverResource }}
    {{- with .Values.bubble.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
{{- end }}
