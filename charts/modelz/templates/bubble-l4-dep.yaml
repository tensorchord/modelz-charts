{{- if .Values.bubble.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.priorityClass.bubbleGPUL4 }}
  labels:
    app: {{ template "modelz.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: {{ .Values.priorityClass.bubbleGPUL4 }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
spec:
  replicas: 0
  selector:
    matchLabels:
      app: {{ .Values.priorityClass.bubbleGPUL4 }}
  template:
    metadata:
      labels:
        service: {{ .Values.priorityClass.bubbleGPUL4 }}
        app: {{ .Values.priorityClass.bubbleGPUL4 }}
    spec:
      priorityClassName: {{ .Values.priorityClass.bubbleGPUL4 }}
      containers:
      - image: {{ .Values.bubble.image }}
        imagePullPolicy: IfNotPresent
        name: victims
        resources:
          requests:
            cpu: 6500m
            memory: 24Gi
            nvidia.com/gpu: 1
          limits:
            nvidia.com/gpu: 1
      nodeSelector:
        ai.tensorchord.server-resource: {{ .Values.bubble.gpuL4Config.serverResource }}
    {{- with .Values.bubble.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
{{- end }}
